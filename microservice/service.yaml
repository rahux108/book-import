apiVersion: v1
kind: Service
metadata:
  name: microservice
  labels:
    name: microservice
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    name: microservice
